#ifndef MAINWINDOW_H
#define MAINWINDOW_H

#include <QMainWindow>
#include <QtWidgets>

class MainWindow : public QMainWindow
{
    Q_OBJECT

public:
    MainWindow(QWidget *parent = 0);
    ~MainWindow();
    
    void createMenuBar();
    
private:
    // file menu
    QMenuBar *menuBar;
    QMenu *fileMenu;
    QAction *exitAction;
};

#endif // MAINWINDOW_H
